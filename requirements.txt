# Deprecated — use per-package pyproject.toml under packages/ and apps/.
# Workspace dev tools: pip install -e ".[dev]" from repo root (after installing packages).
# See README.md
#
# The cost-aware BigQuery GDELT path depends on google-cloud-bigquery, declared
# in packages/news_data/pyproject.toml. It is imported lazily, so it is only
# required when actually running BigQuery queries:
#   pip install "google-cloud-bigquery>=3.0"
