"""Deterministic data manipulation.

If the same input and the same configuration always produce the same output, and
no trained model or model artifact is involved, it belongs here — even when the
output is a score.
"""
