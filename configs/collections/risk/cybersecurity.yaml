name: cybersecurity

providers:
  news:
    gdelt:
      timeout_s: 30
      doc:
        base_url: "https://api.gdeltproject.org/api/v2/doc/doc"
        mode: "artlist"
        format: "json"
        timespan: "12h"
        maxrecords: 50
        sort: "datedesc"

pipeline:
  ingest:
    source: gdelt_docs
    query: "(cyberattack OR ransomware OR \"data breach\" OR cybersecurity OR \"critical infrastructure hack\" OR \"cyber security incident\")"
    max_records: 50

  transform:
    - type: filter_articles
      language: "English"

    - type: dedupe_articles
      dedupe_strategy: "title"

    - type: tag_articles

    - type: aggregate_article_features

    - type: store_features

    - type: store_articles
      output_path: "data/processed/articles/articles.jsonl"
