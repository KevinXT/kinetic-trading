"""
Market data provider integrations (feeds, historical loaders, normalizers).

Status: architectural placeholder. This package boundary is reserved for future
market data provider clients (broker APIs, exchange feeds, vendor integrations).
No implementation exists yet — this is intentional.

When populated, this package should follow the same pattern as ``news_data``:
provider-specific subpackages, config-driven clients, shared schemas.
"""
